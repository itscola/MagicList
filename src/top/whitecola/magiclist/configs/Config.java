package top.whitecola.magiclist.configs;

public class Config {
    /**saveMode
     * json or mysql. json saving mode is default.
     */
    public String savingMode = "json";
    /**
     * savingUUID
     * %aotu% 是自动判断
     * true 存储
     * false 不存储
     */
    public String isSavingUUID = "%auto%";
}
